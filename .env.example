# --- Base de datos compartida (padrón acceso) ---
# Usar las MISMAS credenciales que las otras apps (base controletiquetas).
DB_SERVER=
DB_NAME=
DB_USER=
DB_PASS=
DB_PORT=1433
DB_ENCRYPT=true

# --- Desarrollo local ---
# Email con el que se simula el login cuando no hay Easy Auth delante.
DEV_EMAIL=roberto.sanabria@offal.com.ar

# --- URLs de las apps (opcional; si no, se usan las del catálogo) ---
# URL_PROVEEDORES=
# URL_ETIQUETAS=
# URL_LAVADOS=
